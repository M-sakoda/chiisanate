const swiper = new Swiper(".fv-swiper", {
    effect: "fade",
    fadeEffect: {
        crossFade: true,
    },

    loop: true,

    autoplay: {
        delay: 4000,
        disableOnInteraction: false,
    },

    speed: 2000,
});